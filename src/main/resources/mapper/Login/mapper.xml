<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.peerv2.repository.login.loginRepo">

    <insert id="insertMember">
        INSERT INTO user_info (USER_EMAIL, USER_PASS, USER_NAME, USER_GENDER, USER_AGE, USER_PHONE, USER_NICKNAME,
                               USER_REGE, use_ny, USER_AUTHORITY, USER_BIRTH)
        VALUES (#{user_email}, #{user_pass}, #{user_name}, #{user_gender}, #{user_age}, #{user_phone}, #{user_nickname},
                sysdate() ,"Y", #{user_authority}, #{user_birth})
    </insert>

    <select id="getUserInfoById" resultType="com.example.peerv2.dto.user.userDto">
        select * from user_info where USER_EMAIL =  #{user_email}
    </select>

</mapper>